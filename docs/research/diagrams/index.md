# Diagram Index

One line per export, oldest first. This is what a judge reads next to the image.

| File | Date | What It Shows | Why It Changed From The Last One |
| ---- | ---- | ------------- | -------------------------------- |
| `start-here.canvas` | 2026-09-01 | The scaffold: which problem statement, and the questions to run at each | First version. Structure only, no content |
| `ideation-mindmap.canvas` | 2026-09-06 | Six days of ideation drawn as a tree: what we claimed, what got built, the three competitor scans, the four branches we dropped, and the 30 marks still unclaimed | First real mindmap. Fills the scaffold with what actually happened, and puts the dropped branches on the page instead of only in `../decisions/dropped.md` |
| `user-flow.canvas` | 2026-09-06 | What a group actually does, stage 0 to stage 8, and the loop back from "it broke" to "the plan re-derives" | The mindmap shows how the thinking moved; this shows the product it produced. Together they are the mindmap-plus-user-flow pairing the rubric's top band asks for |


## Exports so far

| Export | Date | Size | From |
| ------ | ---- | ---- | ---- |
| `exports/2026-09-06-user-flow.svg` | 2026-09-06 | 5120 x 1461 | `user-flow.canvas` |
| `exports/2026-09-06-ideation-mindmap.svg` | 2026-09-06 | 3600 x 4490 | `ideation-mindmap.canvas` |

Open **`preview.html`** in a browser to look at either one with pan and zoom, and to save a PNG.
Regenerating is `python docs/diagrams/build/build.py` - see `build/README.md`.
