name: Deploy Prototype

# Every merge to the default branch deploys, whoever pushed it. There is no path
# filter on purpose: a filter means a change lands and nothing happens, silently,
# which is worse than an occasional no-op build.
on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-prototype
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

env:
  PROJECT_ID: codenection-2026
  REGION: asia-southeast1
  SERVICE: prototype
  REPO: web

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: projects/1076024304872/locations/global/workloadIdentityPools/github/providers/tolonglabs
          service_account: deployer@codenection-2026.iam.gserviceaccount.com

      - uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev --quiet

      - name: Build and push
        run: |
          IMAGE="${REGION}-docker.pkg.dev/${PROJECT_ID}/${REPO}/${SERVICE}:${GITHUB_SHA}"
          docker build -t "$IMAGE" .
          docker push "$IMAGE"
          echo "IMAGE=$IMAGE" >> "$GITHUB_ENV"

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy "$SERVICE" \
            --image "$IMAGE" \
            --region "$REGION" \
            --project "$PROJECT_ID" \
            --allow-unauthenticated \
            --port 8080 \
            --quiet

      - name: Report URL
        run: |
          URL=$(gcloud run services describe "$SERVICE" --region "$REGION" --project "$PROJECT_ID" --format='value(status.url)')
          echo "Deployed to $URL" >> "$GITHUB_STEP_SUMMARY"
